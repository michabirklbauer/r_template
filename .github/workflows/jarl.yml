name: Lint with Jarl

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  jarl:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: etiennebacher/setup-jarl@v0.1.1
